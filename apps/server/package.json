{
  "name": "@repo/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/main.ts",
    "build": "npx tsc",
    "clean": "rm -rf dist",
    "start": "node dist/main.js",
    "lint": "bun x eslint .",
    "typecheck": "npx tsc --noEmit"
  },
  "dependencies": {
    "@repo/engine": "*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.9.3"
  }
}
