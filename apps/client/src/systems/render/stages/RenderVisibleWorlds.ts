/**
 * Legacy placeholder kept temporarily while migrating to pass-based rendering.
 * Use the pass modules in `systems/render/passes` instead.
 */
export function RenderVisibleWorldsStage(): void {
  // no-op
}
