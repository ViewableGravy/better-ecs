{
  "name": "@repo/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "clean": "rm -rf dist",
    "preview": "vite preview",
    "lint": "bun x eslint .",
    "typecheck": "npx tsc --noEmit"
  },
  "dependencies": {
    "@repo/engine": "*",
    "@repo/fps": "*",
    "@repo/physics": "*",
    "@repo/spatial-contexts": "*",
    "@standard-schema/spec": "^1.1.0",
    "tiny-invariant": "^1.3.3",
    "zod": "^4.3.4"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.0.0",
    "vite-tsconfig-paths": "^6.1.1"
  }
}
