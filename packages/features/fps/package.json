{
  "name": "@repo/fps",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "bun x tsgo -p tsconfig.lib.json",
    "clean": "rm -rf dist tsconfig.lib.tsbuildinfo",
    "lint": "bun x oxlint .",
    "typecheck": "bun x tsgo --noEmit",
    "test": "bun x vitest run --passWithNoTests"
  },
  "dependencies": {
    "@repo/engine": "workspace:*",
    "tiny-invariant": "^1.3.3",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@standard-schema/spec": "^1.1.0",
    "typescript": "npm:@typescript/native-preview@latest",
    "vitest": "^4.0.0"
  }
}
