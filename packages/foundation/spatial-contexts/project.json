{
  "name": "spatial-contexts",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/foundation/spatial-contexts/src",
  "projectType": "library",
  "tags": ["type:foundation", "domain:spatial-contexts"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x tsc -p tsconfig.lib.json",
        "cwd": "packages/foundation/spatial-contexts"
      },
      "inputs": ["default", "^default"],
      "outputs": ["{projectRoot}/dist"]
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist tsconfig.lib.tsbuildinfo",
        "cwd": "packages/foundation/spatial-contexts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x tsc --noEmit",
        "cwd": "packages/foundation/spatial-contexts"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x tsc --noEmit",
        "cwd": "packages/foundation/spatial-contexts"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x vitest run",
        "cwd": "packages/foundation/spatial-contexts"
      }
    }
  }
}
