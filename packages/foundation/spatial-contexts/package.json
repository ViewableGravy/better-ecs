{
  "name": "@repo/spatial-contexts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./portal": {
      "types": "./src/portal/index.ts",
      "default": "./src/portal/index.ts"
    }
  },
  "scripts": {
    "build": "bun x tsgo -p tsconfig.lib.json",
    "clean": "rm -rf dist tsconfig.lib.tsbuildinfo",
    "lint": "bun x oxlint .",
    "typecheck": "bun x tsgo --noEmit",
    "test": "bun x vitest run"
  },
  "dependencies": {
    "@repo/engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "npm:@typescript/native-preview@latest",
    "vitest": "^4.0.0"
  }
}
