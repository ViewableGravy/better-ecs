# test-package

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build test-package` to build the library.

## Running unit tests

Run `nx test test-package` to execute the unit tests via [Vitest](https://vitest.dev/).
