{
  "name": "utils",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/utils/src",
  "projectType": "library",
  "tags": ["type:utils"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x tsgo -p tsconfig.lib.json",
        "cwd": "packages/utils"
      },
      "inputs": ["default", "^default"],
      "outputs": ["{projectRoot}/dist"]
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist tsconfig.lib.tsbuildinfo",
        "cwd": "packages/utils"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x oxlint .",
        "cwd": "packages/utils"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x tsgo --noEmit",
        "cwd": "packages/utils"
      }
    }
  }
}
