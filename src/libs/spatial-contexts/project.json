{
  "name": "spatial-contexts",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "src/libs/spatial-contexts",
  "projectType": "library",
  "tags": ["type:foundation", "domain:spatial-contexts"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x tsc -p tsconfig.lib.json --emitDeclarationOnly",
        "cwd": "src/libs/spatial-contexts"
      },
      "inputs": ["default", "^default"]
    },
    "build:watch": {
      "continuous": true,
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x tsc -p tsconfig.lib.json --watch",
        "cwd": "src/libs/spatial-contexts"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf .types dist tsconfig.lib.tsbuildinfo",
        "cwd": "src/libs/spatial-contexts"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x eslint .",
        "cwd": "src/libs/spatial-contexts"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x tsc --noEmit",
        "cwd": "src/libs/spatial-contexts"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x vitest run",
        "cwd": "src/libs/spatial-contexts"
      }
    }
  }
}
