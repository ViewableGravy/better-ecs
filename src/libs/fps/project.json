{
  "name": "fps",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "src/libs/fps/src",
  "projectType": "library",
  "tags": ["type:feature", "domain:fps"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist && bun x tsc -p tsconfig.lib.json",
        "cwd": "src/libs/fps"
      },
      "outputs": ["{projectRoot}/dist"],
      "inputs": ["default", "^default"]
    },
    "build:watch": {
      "continuous": true,
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x tsc -p tsconfig.lib.json --watch",
        "cwd": "src/libs/fps"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist",
        "cwd": "src/libs/fps"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x eslint .",
        "cwd": "src/libs/fps"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x tsc --noEmit",
        "cwd": "src/libs/fps"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun x vitest run --passWithNoTests",
        "cwd": "src/libs/fps"
      }
    }
  }
}
